📘 CMS README v4.0
## 🚀 專案簡介
本 CMS 系統採用 PHP OOP + MVC 架構，前後端分離設計，使用 JWT 驗證 控制登入與角色權限。前端以 HTML/JS + DataTables 呈現，後端以 API + Models + Helpers 提供資料存取，確保系統可維護性與擴展性。

## 📂 專案結構
程式碼
cms/
├── api/                      # Controllers (HTTP endpoints)
│   ├── auth_login.php
│   ├── users_get.php
│   ├── users_add.php
│   ├── users_update.php
│   ├── users_delete.php
│   ├── products_get.php
│   ├── products_add.php
│   ├── products_update.php
│   └── products_delete.php
├── models/                   # Business logic (DB access via helpers)
│   ├── LoginModel.php
│   ├── UserModel.php
│   └── ProductModel.php
├── js/ 
│   └── JwtHelper.js  
├── helpers/                  # Reusable helpers
│   ├── Database.php
│   └── JwtHelper.php 
├── pages/                   # Frontend pages & assets
│   ├── index.php
│   ├── weblogin.php
│   ├── users.php
│   ├── products.php
│   ├── js/
│   │   ├── users.js
│   │   ├── products.js
│   │   ├── index.js   
│   │   └── weblogin.js        
└── config/
    └── config.php            # DB 連線設定、JWT secret
## 🔑 功能特色
登入驗證：使用 LoginModel 驗證帳密，成功後生成 JWT。

JWT 驗證：所有 API 需帶 Authorization: Bearer <token>。

角色控制 (RBAC)：JWT payload 包含 role，前端根據角色顯示不同功能。

用戶管理：CRUD 操作（新增、更新、刪除、查詢）。

產品管理：CRUD 操作，支援 DataTables 顯示。

前端整合：使用 ApiHelper.js 統一 API 呼叫，JwtHelper.js 處理 token。

安全性：密碼使用 password_hash() + password_verify()。

## 🔄 資料流流程
登入流程

前端 login.php → 提交帳密 → api/login.php

後端驗證 → 生成 JWT → 回傳 {token, user}

前端存入 localStorage.token

受保護資源

前端呼叫 API → 帶 Authorization: Bearer <token>

後端 JwtHelper::verifyToken() → 驗證成功 → 呼叫 Model

Model 使用 Database 執行 SQL → 回傳資料

Controller echo json_encode(data) → 前端渲染

## 🖼 架構流程圖
mermaid
flowchart TD
    A[Frontend Pages (users.php, products.php)] --> B[JavaScript (users.js, products.js)]
    B -->|fetch API + JWT| C[Controllers (api/*.php)]
    C -->|驗證 JWT| D[JwtHelper]
    C -->|呼叫 Model| E[Models (UserModel, ProductModel, LoginModel)]
    E -->|SQL 查詢| F[Database Helper (PDO)]
    F --> G[(MySQL Database)]
    C -->|回傳 JSON| B
    B -->|渲染 DataTables| A
📊 API Response 格式
統一 JSON 格式：

json
{
  "status": "success",
  "data": [...]
}
或

json
{
  "status": "error",
  "message": "錯誤原因"
}
## 🛠 技術棧
Backend: PHP 8, PDO, OOP, MVC

Frontend: HTML, CSS, JavaScript, jQuery, DataTables

Auth: JWT (HS256)

Database: MySQL

Helpers: Database, JwtHelper, ResponseHelper

## 📌 下一步計劃
[ ] 加入 統一錯誤處理（ResponseHelper）

[ ] API 支援 分頁 / 搜尋

[ ] 加入 產品圖片上傳

[ ] 撰寫 完整單元測試

[ ] Docker 化部署